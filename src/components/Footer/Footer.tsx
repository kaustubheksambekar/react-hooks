import React from 'react';
import styles from './Footer.module.scss';

function Footer() {
	return (
		<div className={styles.footerWrapper}>
			<span className='pr-2'>Footer</span>
		</div>
	);
}

export default Footer;
